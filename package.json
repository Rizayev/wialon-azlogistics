{
  "name": "wialon-azlogistics",
  "private": true,
  "version": "1.0.0",
  "description": "Merged Wialon chronology report (Xronologiya-Yeni) UI for AZLogistika",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm:dev -w server\" \"npm:dev -w client\"",
    "build": "npm run build -w client && npm run build -w server",
    "start": "node server/dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
